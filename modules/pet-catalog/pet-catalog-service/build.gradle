dependencies {
	compileOnly group: "com.liferay.portal", name: "release.portal.api"
	compileOnly project(":modules:pet-catalog:pet-catalog-api")
}

buildService {
	apiDir = "../pet-catalog-api/src/main/java"
}

group = "ru.sample.pet.catalog"